<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Game Title</title>
    <link rel="icon" type="image/png" href="/assets/profileimages/default.png">
    <script src="/js/tailwind.js"></script>
</head>
<body class="bg-primary flex flex-col items-center justify-start min-h-screen overflow-x-auto">

    <div id="main" class="bg-secondary w-screen h-screen flex items-center justify-center overflow-hidden">
        <div id="paralaxed" class="relative w-full h-full">
            <img id="bg-image" src="/assets/landing/tempbackground.png"
                 alt="Paralaxed Sci-fi Cityscape Background"
                 class="absolute top-0 left-0 h-screen w-auto object-cover"
                 style="image-rendering: pixelated; z-index: 1;">
    
            <img id="distant-image" src="/assets/landing/tempdistant.png"
                 alt="Paralaxed Sci-fi Cityscape Distant"
                 class="absolute top-0 left-0 h-screen w-auto object-cover"
                 style="image-rendering: pixelated; z-index: 2;">
    
            <img id="near-image" src="/assets/landing/tempnear.png"
                 alt="Paralaxed Sci-fi Cityscape Near"
                 class="absolute top-0 left-0 h-screen w-auto object-cover"
                 style="image-rendering: pixelated; z-index: 3;">
    
            <img id="foreground-image" src="/assets/landing/tempforeground.png"
                 alt="Paralaxed Sci-fi Cityscape Foreground"
                 class="absolute top-0 left-0 h-screen w-auto object-cover"
                 style="image-rendering: pixelated; z-index: 4;">
                
            <div id="titleAndPlay" class="absolute inset-0 flex flex-col items-center justify-center text-center text-white z-10">
                <img src="/assets/landing/title.png" alt="Game Title" class="w-[500px]" style="image-rendering: pixelated;">
                <div class="space-y-4">
                    <a href="/home" class="-mt-8 block m-4 px-6 py-3 bg-green-400 text-primary hover:bg-green-500 transition border-r-4 border-b-4 border-primary">PLAY NOW</a>
                </div>
            </div>
        </div>
    </div>     
    
    <div id="second" class="bg-primary sm:w-screen md:w-1/2 flex justify-center p-4 flex-col">
        <div class="flex flex-col lg:flex-row">
            <div class="flex flex-col">
                <p class="text-white text-xl mb-4 font-bold">{GAME TITLE}</p>
                <p class="text-white my-4">Turn Based Player-Versus-Player Strategy Tactics Game. Take Control of several units to defeat you opponent.</p>
            </div>
            <img src="/assets/landing/example.png"
            alt="Example Game Scene"
            style="image-rendering: pixelated;">
        </div>

        <div class="flex flex-col lg:flex-row">
            <div class="flex flex-col">
                <p class="text-white text-xl my-4 font-bold">Play Against Others</p>
                <p class="text-white mb-4">Match up against players from around the world, or create a private match for you and your friends.</p>
            </div>
            <img src="/assets/landing/example.png"
            alt="Example Game Scene"
            style="image-rendering: pixelated;">
        </div>
        <div class="flex items-center justify-center">
            <a href="/home" class="block m-4 px-6 py-3 bg-green-400 text-primary hover:bg-green-500 transition border-r-4 border-b-4 border-primary text-center w-full lg:w-1/4">PLAY NOW</a>
        </div>
    </div>

    <script>
        const bgImage = document.getElementById('bg-image');
        const distantImage = document.getElementById('distant-image');
        const nearImage = document.getElementById('near-image');
        const titleAndPlay = document.getElementById('titleAndPlay');

        function handleParallax() {
            const scrollY = window.scrollY;

            bgImage.style.transform = `translateY(${scrollY * 0.3}px)`; // Slow movement for background
            distantImage.style.transform = `translateY(${scrollY * 0.5}px)`; // Faster for distant layer
            nearImage.style.transform = `translateY(${scrollY * 0.7}px)`; // Even faster for near layer
            //titleAndPlay.style.transform = `translateY(${scrollY * 0.9}px)`; // Fastest for foreground
        }

        window.addEventListener('scroll', handleParallax);
    </script>

</body>
<%- include('partials/footer') %>
</html>
